/*
 * 방치형 화면 (TASK-WM-406).
 *
 * 사용자 방향: 세계관 정하기 전이라 <기하학적 모양>으로 간다.
 * 값이 아니라 뜻으로 이름 짓는다 — 팔레트가 한 자리에 산다.
 *
 * ★ 큰 짜임 (2026-08-18) = 사냥 HUD 칩 한 줄 + 관리판 두 칸 (기지·강화 / 창고·장비).
 *   영웅·환생은 상단 레일이 여는 장. 칩 전체가 손때리기 대상.
 *
 * ★ 해상도 — 고정 픽셀 크기를 최소로 쓰고 flex 로 늘린다.
 *   좁은 창에서는 관리판 두 칸이 아래로 접힌다(.idle-manage 의 wrap).
 */

.idle-root {
	flex-grow: 1;
	position: relative;
	background-color: rgb(14, 15, 20);
	color: rgb(226, 228, 236);
}

.idle-topbar {
	flex-direction: row;
	align-items: center;
	/* 오른쪽 여백 = 진단 스탬프(BuildStampOverlay, 우상단 상주) 자리 — 레일과 안 겹치게 */
	padding: 10px 110px 10px 16px;
	background-color: rgb(20, 22, 29);
	border-bottom-width: 1px;
	border-bottom-color: rgb(38, 41, 52);
	flex-wrap: wrap;
}

.idle-top-stage {
	font-size: 15px;
	-unity-font-style: bold;
	color: rgb(150, 176, 208);
	margin-right: 18px;
}

.idle-top-resource {
	font-size: 20px;
	-unity-font-style: bold;
	flex-grow: 1;
}

.idle-top-note {
	font-size: 12px;
	color: rgb(128, 132, 146);
	flex-shrink: 1;
	white-space: normal;
}

.idle-rail-button {
	margin-left: 8px;
	height: 28px;
	padding: 0 14px;
	font-size: 12px;
}

.idle-manage {
	flex-grow: 1;
	flex-direction: row;
	flex-wrap: wrap;
	min-height: 240px;
}

/* 사냥 칩 — 자동전투 HUD 한 줄. 칩 전체가 손때리기 대상. */
.idle-chip {
	height: 72px;
	flex-shrink: 0;
	flex-direction: row;
	align-items: center;
	padding: 0 14px;
	background-color: rgb(17, 19, 25);
	border-bottom-width: 1px;
	border-bottom-color: rgb(38, 41, 52);
	overflow: hidden;
}

.idle-chip-info {
	flex-grow: 1;
	margin-left: 10px;
	justify-content: center;
}

.idle-live {
	flex-basis: 220px;
	flex-grow: 1;
	min-height: 200px;
	padding: 10px 12px;
	background-color: rgb(17, 19, 25);
	border-left-width: 1px;
	border-left-color: rgb(30, 33, 42);
	overflow: hidden;
}

.idle-shop {
	flex-grow: 1;
	min-height: 72px;
	margin-top: 8px;
}


.idle-hero-sheet {
	position: absolute;
	left: 0;
	right: 0;
	top: 0;
	bottom: 0;
	background-color: rgb(11, 12, 16);
	padding: 14px 18px;
}

.idle-hero-sheet-head {
	flex-direction: row;
	align-items: center;
	justify-content: space-between;
	/* 오른쪽 여백 = 진단 스탬프 자리 (topbar 와 같은 이유) */
	padding-right: 96px;
	margin-bottom: 8px;
}

.idle-hover-tip {
	position: absolute;
	background-color: rgb(18, 20, 28);
	border-width: 1px;
	border-color: rgb(90, 110, 140);
	padding: 6px 8px;
	font-size: 12px;
	color: rgb(226, 228, 236);
	white-space: normal;
	max-width: 220px;
}


.idle-worn-strip {
	flex-direction: row;
	margin: 6px 0 4px 0;
}

.idle-worn-cell {
	flex-grow: 1;
	align-items: center;
	margin: 0 3px;
}

.idle-worn-pip {
	width: 22px;
	height: 22px;
}

.idle-worn-name {
	font-size: 10px;
	color: rgb(150, 176, 208);
	margin-top: 2px;
}

.idle-live-title {
	font-size: 11px;
	color: rgb(120, 126, 142);
	-unity-font-style: bold;
	margin-bottom: 6px;
}

.idle-live-note {
	font-size: 11px;
	color: rgb(150, 176, 208);
	margin-left: 8px;
	white-space: normal;
}

.idle-spacer {
	flex-grow: 1;
}

.idle-vault-head {
	flex-direction: row;
	align-items: center;
}

.idle-vault-mark {
	width: 22px;
	height: 22px;
}

.idle-producer-strip {
	flex-direction: row;
	justify-content: space-between;
	margin-top: 8px;
}

.idle-strip-shape {
	flex-grow: 1;
	height: 26px;
	margin: 0 1px;
}

.idle-hero-row {
	flex-direction: row;
	align-items: center;
}

.idle-hero {
	width: 22px;
	height: 22px;
	margin: 0 3px;
}

.idle-stage-box {
	width: 52px;
	height: 52px;
	margin-left: 16px;
	flex-shrink: 0;
	align-items: center;
	justify-content: center;
}

.idle-shape {
	position: absolute;
	width: 100%;
	height: 100%;
}

.idle-arena-caption {
	font-size: 10px;
	color: rgb(128, 132, 146);
	margin-top: 2px;
}

.idle-health {
	width: 180px;
	height: 8px;
	margin-top: 0;
}

/* 기본 ProgressBar 는 밝은 회색이라 판에서 혼자 하얗다 — 팔레트로 눌러 준다. */
.idle-health .unity-progress-bar__background {
	background-color: rgb(30, 33, 42);
	border-width: 0;
}

.idle-health .unity-progress-bar__progress {
	background-color: rgb(150, 176, 208);
	margin: 0;
}

.idle-health .unity-progress-bar__title {
	font-size: 8px;
	color: rgb(226, 228, 236);
}

.idle-kills-dots {
	flex-direction: row;
	margin-top: 4px;
}

.idle-dot {
	width: 8px;
	height: 8px;
	margin: 0 2px;
	border-radius: 4px;
	background-color: rgb(48, 52, 64);
}

.idle-dot--done {
	background-color: rgb(150, 176, 208);
}

.idle-vault-grid {
	flex-direction: row;
	flex-wrap: wrap;
	margin-top: 8px;
}

.idle-vault-cell {
	width: 18px;
	height: 18px;
	margin: 2px;
}

.idle-page {
	padding-bottom: 6px;
}

.idle-row-title {
	font-size: 13px;
	color: rgb(196, 200, 212);
	margin-top: 10px;
}

.idle-row-value {
	font-size: 11px;
	color: rgb(128, 132, 146);
	margin-bottom: 4px;
}

.idle-button {
	height: 32px;
	font-size: 13px;
	background-color: rgb(38, 44, 58);
	border-width: 0;
	color: rgb(226, 228, 236);
	margin: 2px 0;
}

.idle-button:hover {
	background-color: rgb(48, 56, 74);
}

.idle-button:active {
	background-color: rgb(70, 84, 110);
	scale: 0.97 0.97;
}

.idle-button:disabled {
	opacity: 0.45;
}

.idle-button--strong {
	background-color: rgb(58, 78, 112);
}

.idle-button--ready {
	border-left-width: 3px;
	border-left-color: rgb(122, 168, 232);
}

.idle-button--locked {
	background-color: rgb(26, 29, 38);
	color: rgb(128, 132, 146);
	border-left-width: 3px;
	border-left-color: rgb(52, 57, 70);
}

.idle-note {
	font-size: 11px;
	color: rgb(196, 176, 120);
	margin-top: 6px;
	min-height: 16px;
	white-space: normal;
}

.idle-divider {
	height: 1px;
	background-color: rgb(38, 41, 52);
	margin: 12px 0 6px 0;
}

.idle-shape-row {
	flex-direction: row;
	align-items: center;
	margin: 2px 0;
}

.idle-row-shape {
	width: 22px;
	height: 22px;
	margin-right: 8px;
	flex-shrink: 0;
}

.idle-row-button {
	flex-grow: 1;
	margin: 0;
	height: 30px;
	font-size: 12px;
}

.float-text {
	position: absolute;
	font-size: 15px;
	-unity-font-style: bold;
}

.idle-backdrop {
	position: absolute;
	left: 0;
	top: 0;
	right: 0;
	bottom: 0;
}

.idle-party-row {
	flex-direction: row;
	margin: 4px 0 2px 0;
	flex-shrink: 0;
}

.idle-seat {
	flex-grow: 1;
	flex-basis: 0;
	align-items: center;
	margin: 0 3px;
}

.idle-seat-shape {
	width: 34px;
	height: 34px;
	margin-bottom: 2px;
}

.idle-seat-button {
	width: 100%;
	height: 30px;
	font-size: 11px;
	margin: 0;
	white-space: normal;
}

.idle-visitor {
	position: absolute;
	top: 20%;
	width: 30px;
	height: 30px;
}

.idle-surge {
	font-size: 14px;
	-unity-font-style: bold;
	color: rgb(250, 214, 97);
	margin-left: 12px;
	flex-shrink: 0;
}

.idle-guide {
	font-size: 12px;
	color: rgb(196, 176, 120);
	padding: 4px 16px 6px 16px;
	background-color: rgb(20, 22, 29);
	border-bottom-width: 1px;
	border-bottom-color: rgb(38, 41, 52);
	white-space: normal;
}

.idle-warn {
	color: rgb(232, 148, 108);
	-unity-font-style: bold;
}
